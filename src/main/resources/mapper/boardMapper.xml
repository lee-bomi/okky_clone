<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.okkyclone.repository.BoardMapper">
    <select id="selectById" resultType="com.example.okkyclone.Member">
        SELECT
            *
        FROM
             tbl_fruit WHERE no = #{no}
    </select>
</mapper>